interface:
  display_name: "Source of Truth"
  short_description: "Resolve conflicting claims with evidence"
  default_prompt: "Use the source-of-truth skill to resolve this claim from the available project sources."
