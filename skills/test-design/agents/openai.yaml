interface:
  display_name: "Test Design"
  short_description: "Create an approval-ready test design contract"
  default_prompt: "Use $test-design to create an evidence-based test design contract for this request. The requirement may be an Outline link, a Jira issue, a Testmo case or run, or free text; normalize it and verify asserted behavior before designing cases."
